export const meta = {
  name: 'build-apps-batch',
  description: 'Build new apps for uncovered NC strands (one agent per app)',
  phases: [{ title: 'Build', detail: 'one agent authors each new interactive app' }],
}

const TASKS = [{"file": "data-reporting-and-governance-idsr-walkthrough-simulator.html", "prompt": "Author ONE brand-new, self-contained interactive teaching app as a single HTML file for an England school trust (WeST). A real, deployable classroom demo.\n\nWRITE THE FILE TO: `apps/data-reporting-and-governance-idsr-walkthrough-simulator.html`\n\n## What to build\nApp name: IDSR Walkthrough Simulator\nSubject: Data & Analytics  |  Key stage: Reporting and Governance\nNational Curriculum strand it must cover: Navigating the IDSR and Analyse School Performance (ASP)\nGap it fills: No app orientates leaders to IDSR/ASP layout, significance colouring and headline gauges.\nConcept to implement: Annotated interactive replica of an IDSR explaining quintiles, significance shading and what inspectors read first.\n\n\n## Non-negotiable quality bar\n1. Single self-contained HTML file. All CSS and JS inline. NO external CDNs, fonts, libraries or network calls. Works fully offline.\n2. Genuinely interactive with immediate, specific feedback (not reveal-and-read). The pupil manipulates something and the app responds with targeted feedback. Depth over breadth: one excellent interaction beats five shallow ones.\n3. Pitched precisely at Reporting and Governance. Accurate and on-strand for the England National Curriculum (or for newly added subjects, the standard programme of study / exam content for that subject and phase).\n4. Born accessible and touch-ready: Pointer Events (pointerdown/move/up + setPointerCapture) for any drag, touch-action:none on draggables, so it works on tablets and interactive whiteboards. Keyboard operation and ARIA labels/live regions for any canvas/SVG or drag interaction. Mobile-first responsive.\n5. UK English. NO em dashes in visible text (use commas, colons, semicolons, full stops, parentheses).\n6. Demonstration data only. Never imply real pupils; use clearly fictional names. Do NOT add a demo-data banner/modal yourself (one is injected centrally afterwards); write a clean normal page.\n7. Emoji: use HTML hex entities in HTML body; the \\u escape only inside <script> JS strings; CSS content uses a CSS hex escape. Never use the \\u escape in HTML outside scripts.\n8. Editorial clean design: clear hierarchy, whitespace, a short teacher-facing aim line, projector-readable.\n\nAfter writing, re-read the file: valid HTML, balanced <script> tags, ends with </html>, no syntax errors, no external requests.\n\nReturn the structured summary (built=true, a menu title, a one-line catalogue summary, key features, any risks)."}, {"file": "data-reporting-and-governance-confidence-interval-visualiser.html", "prompt": "Author ONE brand-new, self-contained interactive teaching app as a single HTML file for an England school trust (WeST). A real, deployable classroom demo.\n\nWRITE THE FILE TO: `apps/data-reporting-and-governance-confidence-interval-visualiser.html`\n\n## What to build\nApp name: Confidence Interval Visualiser\nSubject: Data & Analytics  |  Key stage: Reporting and Governance\nNational Curriculum strand it must cover: Statistical significance and confidence intervals in accountability data\nGap it fills: No app explains why small cohorts produce wide confidence intervals and unstable measures.\nConcept to implement: Slide cohort size to show how P8 confidence intervals widen and when a result is statistically significant.\n\n\n## Non-negotiable quality bar\n1. Single self-contained HTML file. All CSS and JS inline. NO external CDNs, fonts, libraries or network calls. Works fully offline.\n2. Genuinely interactive with immediate, specific feedback (not reveal-and-read). The pupil manipulates something and the app responds with targeted feedback. Depth over breadth: one excellent interaction beats five shallow ones.\n3. Pitched precisely at Reporting and Governance. Accurate and on-strand for the England National Curriculum (or for newly added subjects, the standard programme of study / exam content for that subject and phase).\n4. Born accessible and touch-ready: Pointer Events (pointerdown/move/up + setPointerCapture) for any drag, touch-action:none on draggables, so it works on tablets and interactive whiteboards. Keyboard operation and ARIA labels/live regions for any canvas/SVG or drag interaction. Mobile-first responsive.\n5. UK English. NO em dashes in visible text (use commas, colons, semicolons, full stops, parentheses).\n6. Demonstration data only. Never imply real pupils; use clearly fictional names. Do NOT add a demo-data banner/modal yourself (one is injected centrally afterwards); write a clean normal page.\n7. Emoji: use HTML hex entities in HTML body; the \\u escape only inside <script> JS strings; CSS content uses a CSS hex escape. Never use the \\u escape in HTML outside scripts.\n8. Editorial clean design: clear hierarchy, whitespace, a short teacher-facing aim line, projector-readable.\n\nAfter writing, re-read the file: valid HTML, balanced <script> tags, ends with </html>, no syntax errors, no external requests.\n\nReturn the structured summary (built=true, a menu title, a one-line catalogue summary, key features, any risks)."}, {"file": "data-reporting-and-governance-governor-data-summary-builder.html", "prompt": "Author ONE brand-new, self-contained interactive teaching app as a single HTML file for an England school trust (WeST). A real, deployable classroom demo.\n\nWRITE THE FILE TO: `apps/data-reporting-and-governance-governor-data-summary-builder.html`\n\n## What to build\nApp name: Governor Data Summary Builder\nSubject: Data & Analytics  |  Key stage: Reporting and Governance\nNational Curriculum strand it must cover: Governor and trust-board data reporting\nGap it fills: No app produces a clear, jargon-controlled governance summary from headline data.\nConcept to implement: Turn headline measures into a one-page RAG-rated board report with plain-English commentary prompts.\n\n\n## Non-negotiable quality bar\n1. Single self-contained HTML file. All CSS and JS inline. NO external CDNs, fonts, libraries or network calls. Works fully offline.\n2. Genuinely interactive with immediate, specific feedback (not reveal-and-read). The pupil manipulates something and the app responds with targeted feedback. Depth over breadth: one excellent interaction beats five shallow ones.\n3. Pitched precisely at Reporting and Governance. Accurate and on-strand for the England National Curriculum (or for newly added subjects, the standard programme of study / exam content for that subject and phase).\n4. Born accessible and touch-ready: Pointer Events (pointerdown/move/up + setPointerCapture) for any drag, touch-action:none on draggables, so it works on tablets and interactive whiteboards. Keyboard operation and ARIA labels/live regions for any canvas/SVG or drag interaction. Mobile-first responsive.\n5. UK English. NO em dashes in visible text (use commas, colons, semicolons, full stops, parentheses).\n6. Demonstration data only. Never imply real pupils; use clearly fictional names. Do NOT add a demo-data banner/modal yourself (one is injected centrally afterwards); write a clean normal page.\n7. Emoji: use HTML hex entities in HTML body; the \\u escape only inside <script> JS strings; CSS content uses a CSS hex escape. Never use the \\u escape in HTML outside scripts.\n8. Editorial clean design: clear hierarchy, whitespace, a short teacher-facing aim line, projector-readable.\n\nAfter writing, re-read the file: valid HTML, balanced <script> tags, ends with </html>, no syntax errors, no external requests.\n\nReturn the structured summary (built=true, a menu title, a one-line catalogue summary, key features, any risks)."}, {"file": "data-reporting-and-governance-honest-charts-lab.html", "prompt": "Author ONE brand-new, self-contained interactive teaching app as a single HTML file for an England school trust (WeST). A real, deployable classroom demo.\n\nWRITE THE FILE TO: `apps/data-reporting-and-governance-honest-charts-lab.html`\n\n## What to build\nApp name: Honest Charts Lab\nSubject: Data & Analytics  |  Key stage: Reporting and Governance\nNational Curriculum strand it must cover: Data visualisation principles and avoiding misleading charts\nGap it fills: No app teaches honest charting (truncated axes, dual axes, cherry-picked baselines).\nConcept to implement: Toggle misleading-chart tricks on and off to train leaders to spot and avoid distortion in their own reporting.\n\n\n## Non-negotiable quality bar\n1. Single self-contained HTML file. All CSS and JS inline. NO external CDNs, fonts, libraries or network calls. Works fully offline.\n2. Genuinely interactive with immediate, specific feedback (not reveal-and-read). The pupil manipulates something and the app responds with targeted feedback. Depth over breadth: one excellent interaction beats five shallow ones.\n3. Pitched precisely at Reporting and Governance. Accurate and on-strand for the England National Curriculum (or for newly added subjects, the standard programme of study / exam content for that subject and phase).\n4. Born accessible and touch-ready: Pointer Events (pointerdown/move/up + setPointerCapture) for any drag, touch-action:none on draggables, so it works on tablets and interactive whiteboards. Keyboard operation and ARIA labels/live regions for any canvas/SVG or drag interaction. Mobile-first responsive.\n5. UK English. NO em dashes in visible text (use commas, colons, semicolons, full stops, parentheses).\n6. Demonstration data only. Never imply real pupils; use clearly fictional names. Do NOT add a demo-data banner/modal yourself (one is injected centrally afterwards); write a clean normal page.\n7. Emoji: use HTML hex entities in HTML body; the \\u escape only inside <script> JS strings; CSS content uses a CSS hex escape. Never use the \\u escape in HTML outside scripts.\n8. Editorial clean design: clear hierarchy, whitespace, a short teacher-facing aim line, projector-readable.\n\nAfter writing, re-read the file: valid HTML, balanced <script> tags, ends with </html>, no syntax errors, no external requests.\n\nReturn the structured summary (built=true, a menu title, a one-line catalogue summary, key features, any risks)."}, {"file": "data-reporting-and-governance-pupil-data-ethics-checker.html", "prompt": "Author ONE brand-new, self-contained interactive teaching app as a single HTML file for an England school trust (WeST). A real, deployable classroom demo.\n\nWRITE THE FILE TO: `apps/data-reporting-and-governance-pupil-data-ethics-checker.html`\n\n## What to build\nApp name: Pupil Data Ethics Checker\nSubject: Data & Analytics  |  Key stage: Reporting and Governance\nNational Curriculum strand it must cover: Data protection, GDPR and ethical handling of pupil data\nGap it fills: No app addresses lawful basis, minimisation and safeguarding when sharing pupil-level data.\nConcept to implement: Scenario-based tool testing lawful basis, minimisation and anonymisation decisions before data is shared.\n\n\n## Non-negotiable quality bar\n1. Single self-contained HTML file. All CSS and JS inline. NO external CDNs, fonts, libraries or network calls. Works fully offline.\n2. Genuinely interactive with immediate, specific feedback (not reveal-and-read). The pupil manipulates something and the app responds with targeted feedback. Depth over breadth: one excellent interaction beats five shallow ones.\n3. Pitched precisely at Reporting and Governance. Accurate and on-strand for the England National Curriculum (or for newly added subjects, the standard programme of study / exam content for that subject and phase).\n4. Born accessible and touch-ready: Pointer Events (pointerdown/move/up + setPointerCapture) for any drag, touch-action:none on draggables, so it works on tablets and interactive whiteboards. Keyboard operation and ARIA labels/live regions for any canvas/SVG or drag interaction. Mobile-first responsive.\n5. UK English. NO em dashes in visible text (use commas, colons, semicolons, full stops, parentheses).\n6. Demonstration data only. Never imply real pupils; use clearly fictional names. Do NOT add a demo-data banner/modal yourself (one is injected centrally afterwards); write a clean normal page.\n7. Emoji: use HTML hex entities in HTML body; the \\u escape only inside <script> JS strings; CSS content uses a CSS hex escape. Never use the \\u escape in HTML outside scripts.\n8. Editorial clean design: clear hierarchy, whitespace, a short teacher-facing aim line, projector-readable.\n\nAfter writing, re-read the file: valid HTML, balanced <script> tags, ends with </html>, no syntax errors, no external requests.\n\nReturn the structured summary (built=true, a menu title, a one-line catalogue summary, key features, any risks)."}];

const SCHEMA = {
  type: 'object',
  required: ['file', 'built', 'summary'],
  properties: {
    file: { type: 'string' },
    built: { type: 'boolean' },
    title: { type: 'string' },
    summary: { type: 'string' },
    keyFeatures: { type: 'array', items: { type: 'string' } },
    risks: { type: 'string' },
  },
}

phase('Build')
const results = await parallel(TASKS.map((t) => () => agent(t.prompt, { label: `build:${t.file}`, phase: 'Build', schema: SCHEMA })))
const ok = results.filter(Boolean)
log(`Built ${ok.length}/${TASKS.length} apps`)
return ok
